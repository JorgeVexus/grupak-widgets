# Changelog - 2026-08-05

## Navbar Menu (`widgets/navbar-menu`)
- **Navigation / UX**: Se actualizó el botón principal "Contáctanos" para dirigir limpiamente a `/contacto`.
- **Navigation / Submenu**: Se agregaron/actualizaron los enlaces del menú desplegable:
  - "Ventas y cotizaciones" -> abre la pestaña *Quiero cotizar*.
  - "Proveeduría de fibra" -> abre la pestaña *Quiero ser proveedor*.
  - "Quiero trabajar en Grupak" -> submenú nuevo que abre la pestaña *Quiero trabajar en Grupak*.

## Productos Secciones (`widgets/productos-secciones`)
- **Bugfix / Media**: Se corrigieron las rutas de las imágenes en la sección de Láminas de cartón corrugado (`swapLaminasStackImages` en `productos-secciones.js`) utilizando los archivos `.webp` exactos almacenados en `widgets/productos-secciones/images/seccion-laminas`.
- **UI / Mobile Typography**: Se mantuvo la medida "283 cm" integrada como parte del párrafo descriptivo en la sección de Láminas en dispositivos móviles, removiendo la elevación a bloque destacado de gran tamaño.

## Productos Interactivos (`widgets/productos-interactivos`)
- **Bugfix / Media**: Se cambiaron las extensiones de `.png` a `.webp` en los atributos `src` del HTML base (`productos-interactivos.html`) para las dos imágenes del stack de Láminas de cartón corrugado.
- **Layout / Structural**: Se corrigió el cierre del contenedor `.laminas-resistencia-box` para restaurar la disposición horizontal en paralelo (`display: flex; gap: 70px;`) dentro de `.laminas-bottom-layout`.

## Productos Menú & Secciones (`widgets/productos-menu`, `widgets/productos-secciones`)
- **Navigation / UX**: Se expuso la función `window.gpkGoToProductsSlide` como alias de `window.gpkGoToProductsSection` y se reconectó el manejador en `productos-menu.js` para permitir la navegación directa entre menús, submenús y secciones de productos.
- **Navigation / Section 2**: Se reconectaron los botones "Saber más" en la Sección 2 (Resumen de productos) (`productos-interactivos.html` y `productos-secciones.js`), vinculando Cajas a `mode 7` y Grabados a `mode 10`.
- **UI / Alignment**:
  - Se removió la línea verde decorativa de los títulos en escritorio en las secciones de *Cajas y empaques* y *Grabados*, mostrándola exclusivamente en móvil.
  - Se reajustó la posición vertical de las 4 tarjetas de *Grabados* en escritorio (`top: 335px` para 1 y 3, `top: 405px` para 2 y 4) y se bajó el título a `top: 95px` para guardar simetría con el resto de las secciones.
  - Se alinearon a la izquierda en escritorio los títulos y descripciones de *Grabados* y *Energía*, y se amplió el ancho de los párrafos en *Cajas - Impresión Digital* (`max-width: 720px`) para guardar simetría con *Cajas convencionales*.

## Gestión Widget (`widgets/gestion`)
- **UI / Component Redesign**: Se transformaron las diapositivas estáticas del slider de imágenes fijas SVG a tarjetas HTML semánticas completas (`.gpk-gestion-card`).
- **Media & Typography**:
  - Se implementaron imágenes WebP de fondo por tarjeta (`images/bg/gestion 01 bg.webp` - `06 bg.webp`) con resolución automática de rutas locales y remotas en `gestion.js`.
  - Se posicionaron los logos oficiales de EDGE Advanced y EPD en la parte superior de las tarjetas 1 y 2.
  - Se extrajo e integró la copia textual exacta desde Figma (nodo `2327-12847`), incluyendo el botón de descarga DAP punteado directamente debajo de los párrafos en la tarjeta 2.
- **Mobile Responsiveness**: Se ajustó el aspect ratio y la altura mínima de las tarjetas en dispositivos móviles (`min-height: 680px-700px`) junto con un degradado protector para garantizar legibilidad completa sobre la fotografía de fondo.

