# Changelog - 2026-08-05

## Navbar Menu (`widgets/navbar-menu`)
- **Navigation / UX**: Se actualizó el botón principal "Contáctanos" para dirigir limpiamente a `/contacto`.
- **Navigation / Submenu**: Se agregaron/actualizaron los enlaces del menú desplegable:
  - "Ventas y cotizaciones" -> abre la pestaña *Quiero cotizar*.
  - "Proveeduría de fibra" -> abre la pestaña *Quiero ser proveedor*.
  - "Quiero trabajar en Grupak" -> submenú nuevo que abre la pestaña *Quiero trabajar en Grupak*.

## Productos Secciones (`widgets/productos-secciones`)
- **UI / Mobile Alignment**: Se vinculó la imagen panorámica de la tarjeta 2 de Energía (*Menor impacto ambiental*) con `background-position: 85% 50%` en móviles, encuadrando exactamente al operador con casco azul y orejeras rojas.
- **Routing & Base URL Resolution**: Se mejoró la resolución de URLs locales (`new URL(".", currentScript.src)`) en `productos-secciones.js` para asegurar la carga fluida de recursos y subplantillas al previsualizar vía Live Server u otros servidores locales.


## Productos Interactivos (`widgets/productos-interactivos`)
- **Bugfix / Media**: Se cambiaron las extensiones de `.png` a `.webp` en los atributos `src` del HTML base (`productos-interactivos.html`) para las dos imágenes del stack de Láminas de cartón corrugado.
- **Layout / Structural**: Se corrigió el cierre del contenedor `.laminas-resistencia-box` para restaurar la disposición horizontal en paralelo (`display: flex; gap: 70px;`) dentro de `.laminas-bottom-layout`.

## Productos Menú & Secciones (`widgets/productos-menu`, `widgets/productos-secciones`)
- **Navigation / UX**: Se expuso la función `window.gpkGoToProductsSlide` como alias de `window.gpkGoToProductsSection` y se reconectó el manejador en `productos-menu.js` para permitir la navegación directa entre menús, submenús y secciones de productos.
- **Navigation / Section 2**: Se reconectaron los botones "Saber más" en la Sección 2 (Resumen de productos) (`productos-interactivos.html` y `productos-secciones.js`), vinculando Cajas a `mode 7` y Grabados a `mode 10`.
- **UI / Alignment**:
  - Se removió la línea verde decorativa de los títulos en escritorio en las secciones de *Cajas y empaques* y *Grabados*, mostrándola exclusivamente en móvil.
  - Se reajustó la posición vertical de las tarjetas de *Grabados* en escritorio (`top: 335px` para 1 y 3, `top: 380px` para 2 y 4) y se bajó el título a `top: 95px` para guardar simetría con el resto de las secciones.
  - Se alinearon a la izquierda en escritorio los títulos y descripciones de *Grabados* y *Energía*, y se amplió el ancho de los párrafos en *Cajas - Impresión Digital* (`max-width: 720px`) para guardar simetría con *Cajas convencionales*.
  - Se protegió con `white-space: nowrap` la frase `y e-commerce.` en *Impresión Digital* para evitar que la letra "e-" quede partida al final de la línea.

## Gestión Widget (`widgets/gestion`)
- **UI / Component Redesign**: Se transformaron las diapositivas estáticas del slider de imágenes fijas SVG a tarjetas HTML semánticas completas (`.gpk-gestion-card`).
- **Media & Typography**:
  - Se implementaron imágenes WebP de fondo por tarjeta (`images/bg/gestion 01 bg.webp` - `06 bg.webp`) con resolución automática de rutas locales y remotas en `gestion.js`.
  - Se posicionaron los logos oficiales de EDGE Advanced y EPD en la parte superior de las tarjetas 1 y 2.
  - Se extrajo e integró la copia textual exacta desde Figma (nodo `2327-12847`), incluyendo el botón de descarga DAP punteado directamente debajo de los párrafos en la tarjeta 2.
- **Responsiveness**: Se ajustó el aspect ratio, altura (`min-height: 750px` en escritorio, `680px-700px` en móvil) y un degradado blanco protector superior para garantizar legibilidad completa sin que el texto tape las imágenes de fondo.
## Formulario Widget (`widgets/formulario`)
- **UI / Color System & Hover States**:
  - Se personalizaron las pestañas de selección con su paleta cromática exclusiva: **Cotización** (`#C97055`), **Proveedor** (`#EF875F`) y **Quiero trabajar en Grupak** (`#8E94D8`).
  - Se configuraron bordes, flechas `→` y títulos para adoptar el color distintivo en estado neutro/hover y responder con fondo sólido de color al estar activas (`.is-active`).
  - Se adaptaron los elementos internos de cada formulario panel (línea inferior decorativa de títulos, estados de enfoque focus en campos, selector de archivos y botones de envío) al color de la pestaña correspondiente.

## Chatbot Flotante (`widgets/chat-flotante`)
- **UI / Fix Header Clipping**: Se removió el pseudo-elemento recortado inferior de la cabecera verde de Valeria (`.gpk-chat__header::after`) y se aplicó `border-radius: 7px 7px 0 0` para una integración continua y pulida con el contenedor del chat.
- **Navigation & Routing**:
  - Se conectaron los botones de **Información de productos** (*Papel*, *Cajas y empaques*, *Lámina de cartón corrugado*, *Grabados*) para desplazarse suavemente a su respectiva sección en el Home.
  - Se añadió lógica de redirección inteligente: cuando se interactúa con el chatbot desde subpáginas externas, redirige al Home e inicia la vista directamente en la sección solicitada (`/?gpkProduct=papel#gpk-ps-widget-root`).
  - Se conectaron las opciones de contacto/formulario a `/contacto?gpkForm=contacto`.

## Más Allá del Empaque (`widgets/mas-alla-del-empaque`)
- **Media Optimization**: Se convirtió y actualizó la imagen 2 del carrusel (*Personas*) a formato WebP optimizado (`mas-alla-02.webp`), sincronizando las referencias HTML en `mas-alla-del-empaque.html`.

