# Registro de Cambios - 2026-06-18

## Resumen de la Sesión
En esta sesión se implementó una animación de trazado de contornos secuencial en la pantalla de inicio (Slide 0) y se modularizó el widget de productos interactivos para su hospedaje en GitHub y despliegue en Vercel. Adicionalmente, se implementó una arquitectura de carga autónoma (auto-loader) en JavaScript para simplificar la integración en Webflow, se configuró el mapeo para las tipografías Neue Haas Display y peso normal (400) sin negritas en todo el widget, y se estandarizaron los títulos principales a 50px y color #6e6e6e. También se ajustaron detalles finos como el fondo a `#d9d9d9`, color `#6e6e6e` en subtítulos y títulos principales que seguían en negro (Papel y Grabados), formato de tarjetas de overview (Slide 1) y texto en negrita localizado para KPIs y descripciones.

## Cambios Detallados

### Componentes y Archivos Creados
- **`widgets/productos-interactivos/`**:
  - `productos-interactivos.html`: Marcado de la sección de productos interactivos de Grupack. Se envolvieron "cajas ranuradas" y "cajas suajadas" en etiquetas `<strong>` dentro de la sección mobile de Slide 2.
  - `productos-interactivos.css`: Hojas de estilos y animaciones. Se actualizaron las variables tipográficas para utilizar `Neuehaasdisplayroman`. Se añadió una regla global para desactivar el texto en negrita (`font-weight: 400 !important`). Se modificaron las clases de títulos `.overview-title-new`, `.pane-title-centered`, `.section-detail-title`, `.energia-main-title`, `.overview-col-title` y `.energia-row h3` para establecer el color en `#6e6e6e` e igualar el tamaño de los títulos principales a `50px` en escritorio. Se ajustó `--color-grey-light` a `#d9d9d9`, se asignaron subtítulos a `#6e6e6e`, se añadieron reglas de negrita localizadas (`strong`/`b`), se configuraron los títulos de columnas del Slide 1 en negrita con línea verde inferior y las descripciones en color verde.
  - `productos-interactivos.js`: Se actualizó para inyectar dinámicamente sus propios estilos CSS e importar el marcado HTML desde Vercel de forma automática al detectar el contenedor raíz.
- **`README.md`**: Se simplificó la documentación de inyección en Webflow, reduciéndola a un contenedor y un único tag de script.
- **`vercel.json`**: Se agregaron cabeceras CORS para habilitar peticiones HTTP fetch asíncronas desde Webflow a Vercel.

### Modificaciones en Archivos Existentes
- **`hermes-preview-productos.html`**:
  - Ajuste de los SVGs de contorno para que calcen perfectamente con las resoluciones nativas de las imágenes.
  - Configuración de la animación coreográfica en `.mode-0` para el trazado escalonado en la pantalla inicial.
  - Sincronización completa de los nuevos ajustes visuales, colores de títulos, fondos y negritas.

## Historial de Commits
- `feat: add products interactive widget and repository readme`
- `chore: enable CORS headers via vercel.json`
- `feat: simplify webflow integration with script-based autoloader`
- `style: configure custom Neue Haas Display fonts for Webflow`
- `style: force Roman font and normal font-weight across all text`
- `style: set main titles to 50px and all titles color to #6e6e6e`
- `style: adjust background color, title colors and typography weights`
