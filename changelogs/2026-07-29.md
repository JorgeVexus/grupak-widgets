# Changelog - 2026-07-29

## Navbar Menu Widget (`widgets/navbar-menu`)
- **Feat**: Añadido enlace de **Certificaciones** con ruta `/certificaciones` en la navegación principal al lado de **Productos**.
- **Feat / Style**: Actualizada la barra base a 100% transparente en el estado superior (`top`), y cambiado el logo por `logoGrupak 2.svg`.
- **Feat / Style**: Añadida la variante de tema de fondo blanco (`data-gpk-theme="white"` / `data-gpk-variant="white"`) con enlaces en verde corporativo (`var(--gpk-green)`).
- **Chore**: Actualizada versión de assets (`assetVersion = "20260729-transparent-white-theme-1"`).

## Productos Interactivos Widget (`widgets/productos-interactivos`)
- **Feat**: Reemplazada la imagen estática del Hero por un video de fondo optimizado (`hero-video.mp4` / `hero-video.webm`).
- **Optimization**: Reducido el peso del video un 65% (de 23.1 MB a 8.1 MB en 720p H.264) con `faststart` activado para transmisión sin pausas y remoción de audio (`-an`).
- **Style / Script**: Aplicada la máscara recortada de 4 canales RGBA (`hero-mask.png`) al video del hero, e inyección dinámica con `maskImage` en JavaScript.
- **Style / Layout**: Reestructurados los KPIs del hero (añadidos 2 en la izquierda y 2 en la derecha) y ajustados los anchos y tipografías para evitar desbordamiento de texto.
