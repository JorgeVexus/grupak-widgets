# Registro de Cambios - 2026-06-22

## Resumen de la Sesión
En esta sesión se completó el rediseño y optimización de los hitos **2021**, **2023** y **2024** del widget interactivo de la línea de tiempo (`widgets/linea-tiempo/`). Además, se rediseñó por completo la experiencia móvil del timeline convirtiéndola de un carrusel de pestañas a un feed de línea de tiempo vertical continuo e interactivo con animaciones premium de scroll:
- **Hito 2021**: Se reestructuró la maquetación a una distribución de dos columnas escalonadas con dos bloques de texto y dos imágenes alternadas. Las imágenes (`2021-01 border.svg` y `2021-02 border.svg`) incorporan máscaras SVG de silueta técnica que se adaptan dinámicamente tanto en escritorio como en móvil.
- **Hito 2023**: Se replicó el marco de silueta de edificio clásico del año 2015, utilizando la nueva imagen `hero-2023.webp` integrada mediante base64 dentro de un SVG unificado con máscara de recorte (`2023 border svg.svg`) para asegurar una visualización perfecta en cualquier resolución.
- **Hito 2024**: Se utilizó la misma máscara de silueta de edificio con la imagen de 2023, pero invirtiendo completamente la maquetación en escritorio (imagen al tope, texto en la parte inferior).
- **Rediseño Móvil (Vertical Feed)**: Se transformó el carrusel de diapositivas horizontales en un flujo de desplazamiento vertical continuo. Se ocultó la barra de botones de años superior y los botones de navegación inferiores. Se agregaron años en la parte izquierda de cada fila mediante pseudo-elementos, una línea vertical verde continua que conecta todos los hitos, y círculos/nodos concéntricos verdes a la altura de cada año. Los contenidos de texto e imágenes se formatearon como tarjetas individuales limpias y legibles con fondo blanco, bordes finos y sombras discretas.
- **Efecto de Llenado de Línea en Scroll (Móvil)**: Se implementó una línea verde activa (`.mobile-active-line`) superpuesta que se va llenando dinámicamente de arriba a abajo conforme el usuario hace scroll hacia abajo en la página, utilizando una transición suave en JavaScript calculada a partir del punto de activación (trigger) en la mitad superior de la pantalla.
- **Animaciones de Entrada de Tarjetas (Móvil)**: Las tarjetas de información y de imágenes inician en estado invisible (`opacity: 0`) y desplazadas hacia abajo (`translateY(30px)`). Al aproximarse al punto de activación en el scroll, se revelan de forma fluida con un deslizamiento hacia arriba (`translateY(0)` y `opacity: 1`). Los nodos de los años inician con fondo gris claro y se rellenan con un color verde brillante y un resplandor cuando la línea del scroll los alcanza, logrando una experiencia sumamente interactiva y pulida.
- **Compilación de la Línea de Tiempo**: Se ejecutó el script de compilación para actualizar la visualización local en `preview-linea-tiempo.html` y el embed en `grupak-linea-tiempo-embed.html`.

## Cambios Detallados

### Línea de Tiempo (`widgets/linea-tiempo/`)
- **`widgets/linea-tiempo/linea-tiempo.html`**:
  - Reemplazados los contenedores vectoriales obsoletos por los contenedores dinámicos `.photo-container-2023` y `.photo-container-2024`.
  - Reestructurado el Slide 2021 con contenedores independientes para los bloques de texto escalonados (`.info-section-2021-1` y `.info-section-2021-2`) y los contenedores de imágenes técnicas (`.image-container-2021-1` y `.image-container-2021-2`).
- **`widgets/linea-tiempo/linea-tiempo.css`**:
  - **Ajustes Desktop**:
    - Añadidos estilos de animación y posicionamiento absoluto para 2021, 2023 y 2024.
    - Configurado el layout de 2024 con la imagen centrada horizontalmente al tope (`top: 150px`, `width: 920px`, `height: 539px`) y la sección de información debajo (`top: 710px`).
    - Añadidas transiciones de clip-path y opacidad sincronizadas para la revelación de imágenes en la activación de cada diapositiva.
  - **Ajustes Mobile**:
    - Reemplazado todo el bloque de la media query móvil (`@media (max-width: 768px)`) para establecer una maquetación de línea de tiempo vertical continua.
    - Habilitadas todas las diapositivas al mismo tiempo (`display: flex !important; opacity: 1 !important; position: relative !important`).
    - Añadidas las etiquetas de años a la izquierda (`::before` con `content: attr(data-year)`).
    - Añadida la línea vertical de fondo (`::after` de color verde con opacidad `0.25`).
    - Añadida la línea activa de progreso verde (`.mobile-active-line`) con cálculo dinámico de altura en scroll.
    - Añadidos los nodos circulares con color gris claro por defecto y relleno verde dinámico en scroll con la clase `.timeline-slide.visible`.
    - Añadidas transiciones de opacidad y desplazamiento vertical (`translateY(30px)` a `translateY(0)`) a las tarjetas de contenido y fotos al activarse con la clase `.visible`.
    - Estilizados los bloques de información (`.info-section`, etc.) como tarjetas blancas de contenido (`padding: 16px`, borde de `1px` y sombra).
    - Estilizadas y adaptadas todas las imágenes para que se muestren de forma responsiva como tarjetas abajo del texto sin cortes ni deformaciones.
    - Implementada inversión por flexbox en la diapositiva 2024 (`flex-direction: column-reverse`) para asegurar que su imagen se muestre arriba de su texto.
- **`widgets/linea-tiempo/linea-tiempo.js`**:
  - Implementada la función de scroll adaptativa `handleMobileScroll` para calcular la altura de llenado de la línea de progreso activa en móvil y añadir la clase `.visible` a las diapositivas conforme entran en la pantalla.
  - Enrutado el listener de scroll para ejecutar `handleScroll` en pantallas mayores a 768px y `handleMobileScroll` en pantallas menores o iguales a 768px.
  - Deshabilitado el soporte de deslizamiento de panel (swipe) en móviles para evitar conflictos en la navegación vertical.

### Mapa de Ubicaciones (`widgets/locations-map/`)
- **Conversión a Widget**: Convertido el archivo HTML de Webflow estático en un widget modular reutilizable y dinámico.
- **`widgets/locations-map/locations-map.html`**:
  - Estructurado el panel izquierdo para la lista de tarjetas de sedes, el campo de búsqueda y los tags de filtro de categoría.
  - Diseñado el panel derecho con la imagen del mapa de fondo, los marcadores (pins) dinámicos y el popup de información emergente (info card).
- **`widgets/locations-map/locations-map.css`**:
  - Implementado el sistema de variables de color de Grupack, fuentes (Outfit/Inter), y radios de tarjeta.
  - Añadido el layout responsivo de rejilla adaptándose a flexbox vertical en tablets y móviles.
  - Incorporadas animaciones de pulsación activa en los pines del mapa y transiciones suaves de entrada en las tarjetas.
  - Configurado el estilo para que los iconos SVG de pines en cada tarjeta tengan el color correspondiente a su categoría (Corporativo, Planta papel, Abastecimiento).
- **`widgets/locations-map/locations-map.js`**:
  - Configurada la base de datos de 9 locaciones sincronizadas con las coordenadas geográficas de los pines.
  - Renombrada la locación `"Abastecimiento Cuautitlán"` a `"Abastecimiento Cuatitán"` para coincidir con la nomenclatura de Figma.
  - Programados eventos dinámicos para buscador de texto (debounce de 150ms), tags de filtrado por categoría y selección cruzada (tarjetas <-> pines del mapa).
  - Implementado el posicionamiento y apertura dinámica de la ventana emergente de detalles sobre el pin activo, incluyendo estado de horario ("Abierto ahora").
- **Compilación de Mapa**: Creado el script de compilación `scratch/compile_locations_map.py` que genera la versión local (`preview-locations-map.html`) y la versión de producción Webflow (`grupak-locations-map-embed.html`) reemplazando URLs de imágenes locales con recursos en la CDN del dominio de Vercel.

