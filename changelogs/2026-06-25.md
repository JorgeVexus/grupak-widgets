# Registro de Cambios - 2026-06-25

## Resumen de la Sesión
En esta sesión se realizaron ajustes de estilos y tipografía en el slide de **Energía** (Slide 9) del widget **Productos Interactivos** para cumplir con los requerimientos visuales y consistencia de marca:

- **Estilo de Títulos en Verde y Negrita**:
  - Se modificaron los títulos principales y secundarios del slide de energía para usar el color verde institucional (`#5F9D2F`) y peso tipográfico en negrita (`font-weight: 700 !important`).

- **Consistencia en Color de Textos descriptivos**:
  - Se cambió el color de las descripciones de las tarjetas (desktop) a gris medio (`#6E6E6E`) en lugar del color gris oscuro casi negro para mantener la coherencia.
  - Se ajustaron las reglas de los elementos `strong` y `b` dentro del slide de energía para mantener el color gris medio (`#6E6E6E`) en lugar de tomar el color negro por defecto.

- **Compilación y Propagación Automática**:
  - Se corrió el script de sincronización `scratch/sync_mobile_to_embeds.py` para regenerar y sincronizar los cambios de CSS en los archivos embed e interactivos del proyecto.

## Cambios Detallados

### Productos Interactivos (`widgets/productos-interactivos/`)
- **`widgets/productos-interactivos/productos-interactivos.css`**:
  - Modificado `#gpk-products-widget .energia-main-title` (desktop y móvil) para aplicar `color: var(--color-green) !important;` y `font-weight: 700 !important;`.
  - Modificado `#gpk-products-widget .energia-row h3` (desktop y móvil) para aplicar `color: var(--color-green) !important;` y `font-weight: 700 !important;`.
  - Modificado `#gpk-products-widget .energia-row p` para cambiar el color a `var(--color-grey-medium) !important;` (#6E6E6E).
  - Añadidas reglas específicas para `#gpk-products-widget .energia-intro-text strong` y elementos `strong`/`b` dentro de `#pane-energia` con `color: var(--color-grey-medium) !important;` y `font-weight: 700 !important;`.
